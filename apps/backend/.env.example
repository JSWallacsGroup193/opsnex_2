# Database Configuration (Replit provides these automatically)
DATABASE_URL=postgresql://user:password@your-neon-db.neon.tech/dbname?sslmode=require
PGHOST=your-db-host
PGPORT=5432
PGUSER=your-db-user
PGPASSWORD=your-db-password
PGDATABASE=your-db-name

# OpenAI API Key (REQUIRED for AI Chat and AI Cost Estimator)
OPENAI_API_KEY=sk-your-openai-api-key-here

# JWT Secret (recommended for production)
JWT_SECRET=your-secure-jwt-secret-here

# Node Environment
NODE_ENV=production

# CORS Configuration (Security: comma-separated list of allowed origins)
# Example for production: ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
# Leave empty for development (defaults to localhost:5000)
ALLOWED_ORIGINS=
